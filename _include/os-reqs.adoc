= 
:allow-uri-read: 


专属主机:: 与其他应用程序共享的主机不支持 Console 代理。该主机必须是专用主机。主机可以采用满足以下规格要求的任何架构：
+
--
* CPU：8 核或 8 个 vCPU
* 内存: 32 GB
* 磁盘空间：建议主机使用 165 GB，具有以下分区要求：
+
** `/opt`：必须有 120 GiB 的可用空间
+
代理使用 `/opt`安装 `/opt/application/netapp`目录及其内容。

** `/var`：必须有 40 GiB 的可用空间
+
Console 代理需要此空间， `/var`因为 Docker 或 Podman 的架构设计是在此目录中创建容器。具体来说，它们将在 `/var/lib/containers/storage`目录中创建容器。外部挂载或符号链接不适用于此空间。





--


* CPU：16 核或 16 个 vCPU
* 内存：64 GB
* 磁盘空间：596 GB（建议使用厚资源调配）
* vSphere 7.0u3 或更高版本，使用虚拟硬件版本 19 或更高版本



NOTE: Console 代理保留 19000 至 19200 的 UID 和 GID 范围。此范围是固定的，无法修改。如果主机上的任何第三方软件正在使用此范围内的 UID 或 GID，则代理安装将失败。NetApp 建议使用没有第三方软件的主机以避免冲突。

NetApp Console 本地部署使用固定地址空间进行服务间通信。IP 范围 10.42.0.0/16 和 10.43.0.0/16 用于内部网络。在部署 Console 本地部署之前，请确保这些 IP 范围未分配给 Console 本地部署可用的 VLAN 上的其他虚拟机、DHCP 范围、DNS 记录或负载均衡器 VIP 池。

有关如何更改代理接口的 IP 地址的说明，请参阅知识库文章 Agent IP conflict with existing network。

虚拟机管理程序:: 需要具有运行受支持操作系统认证的裸机或托管虚拟机管理程序。


[[podman-versions]]操作系统和容器要求:: 在标准模式或限制模式下使用 Console 时，以下操作系统支持 Console 代理。安装代理之前需要容器编排工具。
+
--
[cols="2a,2a,2a,2a,2a"]
|===
| 操作系统 | 支持的操作系统版本 | 支持的代理版本 | 所需的容器工具 | SELinux a 


 a| 
Red Hat Enterprise Linux
 a| 
9.1 至 9.4

8.6 至 8.10

* 仅提供英文版本。
* 主机必须在 Red Hat Subscription Management 中注册。如果未注册，则在安装代理期间，主机无法访问存储库以更新所需的第三方软件。

 a| 
3.9.50 或更高版本，Console 处于标准模式或受限模式
 a| 
Podman 版本 4.6.1 或 4.9.4

<<podman-configuration,查看 Podman 配置要求>>。
 a| 
在强制模式或许可模式下支持

* 操作系统上启用了 SELinux 的代理不支持管理 Cloud Volumes ONTAP 系统。




 a| 
Ubuntu
 a| 
24.04 LTS
 a| 
3.9.45 或更高版本，NetApp Console 处于标准模式或受限模式
 a| 
Docker Engine 23.06 至 28.0.0。
 a| 
不支持



 a| 
22.04 LTS
 a| 
3.9.50 或更高版本
 a| 
Docker Engine 23.0.6 到 28.0.0。
 a| 
不支持

|===
--


[[podman-versions]]操作系统和容器要求:: 在私有模式下使用 Console 时，以下操作系统支持 Console 代理。安装代理之前需要容器编排工具。
+
--
[cols="2a,2a,2a,2a,2a"]
|===
| 操作系统 | 支持的操作系统版本 | 支持的代理版本 | 所需的容器工具 | SELinux 


 a| 
Red Hat Enterprise Linux
 a| 
9.1 至 9.4

8.6 至 8.10
 a| 
3.9.42 或更高版本，Console 处于私有模式
 a| 
Podman 版本 4.6.1 或 4.9.4

<<podman-configuration,查看 Podman 配置要求>>。
 a| 
在强制模式或许可模式下受支持 ^1^



 a| 
Ubuntu
 a| 
22.04 LTS
 a| 
3.9.29 或更高版本
 a| 
Docker Engine 23.0.6 至 26.0.0

26.0.0 支持 _new_ agent 3.9.44 或更高版本的全新安装
 a| 
不支持

|===
注：

. 操作系统上启用了 SELinux 的代理不支持管理 Cloud Volumes ONTAP 系统。
. 这些操作系统的英语版本支持 Console 代理。
. 对于 RHEL，主机必须在 Red Hat Subscription Management 中注册。如果未注册，主机将无法在安装代理期间访问存储库以更新所需的第三方软件。


--


CPU:: 8 核或 8 个 vCPU
RAM:: 32 GB


AWS EC2 实例类型:: 符合上述 CPU 和 RAM 要求的实例类型。建议使用 t3.2xlarge。


密钥对:: 创建 Console 代理时，您需要选择要与实例一起使用的 EC2 密钥对。


使用 IMDSv2 时的 PUT 响应跃点限制:: 如果在 EC2 实例上启用了 IMDSv2（这是新 EC2 实例的默认设置），则必须将实例上的 PUT 响应跃点限制更改为 3。如果不更改 EC2 实例的限制，尝试设置代理时将收到 UI 初始化错误。
+
--
* link:task-require-imdsv2.html["需要在 Amazon EC2 实例上使用 IMDSv2"]
* https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configuring-IMDS-existing-instances.html#modify-PUT-response-hop-limit["AWS 文档：更改 PUT 响应跃点限制"^]


--


Azure VM 大小:: 符合上述 CPU 和 RAM 要求的实例类型。我们建议使用 Standard_D8s_v3。


Google Cloud 机器类型:: 符合上述 CPU 和 RAM 要求的实例类型。建议使用 n2-standard-8。
+
--
在 Google Cloud 上，支持在具有以下操作系统的 VM 实例上使用 Console 代理： https://cloud.google.com/compute/shielded-vm/docs/shielded-vm["Shielded VM 功能"^]

--


/opt 中的磁盘空间:: 必须有 100 GiB 的可用空间
+
--
代理使用 `/opt`安装 `/opt/application/netapp`目录及其内容。

--
/var 中的磁盘空间:: 必须有 20 GiB 的可用空间
+
--
Console 代理需要此空间， `/var`因为 Docker 或 Podman 的架构设计是在此目录中创建容器。具体来说，它们将在 `/var/lib/containers/storage`目录中创建容器。外部挂载或符号链接不适用于此空间。

--

